[Назад](README.md) 
---
# Heap, Stack и GC

## 🔹 1. Основы памяти в Java
**Вопрос:** Что такое Heap и Stack?  
**Ответ:**  
Heap — область памяти для хранения объектов. Stack — для хранения примитивов, ссылок и фреймов вызова методов.

**Вопрос:** Где хранятся примитивы (int, boolean)?  
**Ответ:** В Stack, если они локальные переменные.

**Вопрос:** Где хранятся объекты (new Object())?  
**Ответ:** В Heap. Ссылки на них — в Stack.

**Вопрос:** Что такое String Pool?  
**Ответ:** Это область в Heap, где хранятся строковые литералы. Повторяющиеся строки используют одну память.

**Вопрос:** Что такое PermGen и Metaspace?  
**Ответ:** PermGen — устаревшая область памяти для классов в Java 7 и ниже. Metaspace — замена в Java 8+, живёт в native memory.

**Вопрос:** Как работает intern() у String?  
**Ответ:** Помещает строку в String Pool и возвращает ссылку из пула, если она уже там есть.

**Вопрос:** Что такое StackOverflowError?  
**Ответ:** Переполнение стека вызовов — обычно из-за слишком глубокой рекурсии.

**Вопрос:** Что такое OutOfMemoryError?  
**Ответ:** Ошибка при нехватке памяти в Heap, Metaspace, Stack или Direct Memory.

**Вопрос:** Как увеличить размер Stack?  
**Ответ:** Через параметр `-Xss<size>`, например `-Xss512k`.

## 🔹 2. Garbage Collection (GC) — Основы
**Вопрос:** Как работает Garbage Collection в Java?  
**Ответ:** Через алгоритмы mark-and-sweep, копирование, компактацию и удаление unreachable объектов из Heap.

**Вопрос:** Чем отличается Minor GC от Full GC?  
**Ответ:** Minor — сборка Young Gen, быстрая. Full — весь Heap и Metaspace, долгая.

### 🔄 4. Чем отличается Minor GC от Major и Full GC?
**Ответ:**
- **Minor GC**: работает в Young Generation, обычно быстро.
- **Major GC**: работает в Old Generation, дольше.
- **Full GC**: собирает всю кучу (Young + Old + Metaspace), может быть очень медленным.

### 💥 6. Что такое `OutOfMemoryError: GC overhead limit exceeded`?
**Ответ:** Возникает, если GC тратит >98% времени на сборку и освобождает <2% heap. Признак утечки или слишком маленького Heap.

### 📊 10. Что такое Metaspace и чем отличается от PermGen?
**Ответ:** Metaspace (с Java 8) хранит метаданные классов вне Heap, в native memory. PermGen — устаревшая область памяти, существовавшая до Java 8.

### 🧠 12. Чем отличаются Minor, Major и Full GC?
**Ответ:**
- **Minor GC**: собирает только Young Generation, быстро.
- **Major GC**: собирает Old Generation.
- **Full GC**: собирает всю кучу (Young + Old + Metaspace), может быть долгим и STW.

### 🔹 Вопрос 20: Что такое OutOfMemoryError и какие его типы?
**Ответ:**
Ошибка, когда JVM не может выделить память.
Типы:
- `Java heap space`
- `GC overhead limit exceeded`
- `PermGen space` (до Java 8)
- `Metaspace` (с Java 8)
- `Direct buffer memory` (при использовании NIO)

### 🔹 Вопрос 21: Что такое Metaspace и как её настроить?
**Ответ:**
Metaspace — область памяти вне Heap, где хранятся метаданные классов.
Настройки:
- `-XX:MetaspaceSize`
- `-XX:MaxMetaspaceSize`
Заменяет PermGen начиная с Java 8.

### 🔹 Вопрос 22: Что такое Memory Leak и как его находят?
**Ответ:**
Утечка — объект больше не нужен, но доступен по ссылке.
Способы обнаружения:
- `VisualVM`, `Eclipse MAT`, `jmap`
- Heap dump при `-XX:+HeapDumpOnOutOfMemoryError`
Примеры: статические коллекции, слушатели, кэш без очистки.

### 🧠 34. Что такое GC Overhead Limit Exceeded?
Ошибка `java.lang.OutOfMemoryError: GC overhead limit exceeded` означает:
- >98% времени уходит на GC
- <2% памяти освобождается

### 🧠 35. Что такое Metaspace и как его настраивать?
Metaspace — это область вне heap, в которой хранятся метаданные классов (с Java 8).

Настройки:
- `-XX:MetaspaceSize`
- `-XX:MaxMetaspaceSize`
Использовать `jcmd`, `jmap` или `VisualVM` для мониторинга.

### 💡 41. Что такое GC Overhead Limit Exceeded?
**Ответ:** JVM тратит >98% времени на GC, освобождая <2% памяти. Решение: увеличить Heap, найти утечки.

### 💡 44. Что такое String.intern()?
**Ответ:** Помещает строку в String Pool. Если уже есть такая строка, возвращает ссылку на неё.

### 55. Что такое escape analysis?
Анализ, позволяет JVM понять, "утекает" ли объект за пределы метода. Если нет — может быть выделен в Stack вместо Heap, оптимизируя GC.

### 59. Что делает параметр -XX:+HeapDumpOnOutOfMemoryError?
При OOM JVM создаёт heap dump, который можно анализировать (Eclipse MAT и др).

### 63. Что такое Native Memory?
Память вне heap: DirectByteBuffer, Metaspace, JIT, JNI. Мониторится через NMT.

### 64. Что такое PermGen?
Область памяти до Java 8, где хранились классы, строки и метаданные. Заменена на Metaspace.

### 65. Что такое Metaspace?
С Java 8, заменяет PermGen. Использует native memory. Можно контролировать через `-XX:MaxMetaspaceSize`.

### 74. Как происходит stack allocation?
Объекты могут быть размещены в Stack, если escape analysis показал, что они не выходят за пределы метода.

### 🧪 85. Что такое Remembered Set (RSet) в G1?
RSet — структура, хранящая ссылки из других регионов в текущий.
- Позволяет избежать полного сканирования Heap.
- Ускоряет GC, особенно при наличии большого Old Gen.

### ⏳ 86. Что такое GC Overhead Limit Exceeded?
Исключение: `java.lang.OutOfMemoryError: GC overhead limit exceeded`
- GC тратит >98% времени и освобождает <2% памяти.
- Решение: увеличить `-Xmx`, проанализировать Heap Dump, снизить аллокации.

### 🧱 88. Как DirectByteBuffer влияет на Native Memory и GC?
- Выделяет память вне Heap.
- Может вызвать OOM, если не очищается.
- Нужно использовать try-with-resources или Cleaner.

### 🔄 89. Что такое Card Table?
- Структура, отслеживающая изменения в Heap.
- Каждая "карта" покрывает 512 байт.
- Используется в G1/Parallel GC для оптимизации маркировки.

### 🧠 90. Как размер кучи влияет на частоту GC?
| Размер Heap | Частота GC | Pause Time | CPU Load |
|-------------|------------|-------------|----------|
| < 1 ГБ      | высокая    | малая       | высокая  |
| 1–10 ГБ     | средняя    | средняя     | умеренная|
| >10 ГБ      | низкая     | высокая     | низкая   |

### 📉 94. Что такое Metaspace и как его тюнить?
- В Java 8+ заменила PermGen.
- Хранит метаданные классов.
- Параметры: `-XX:MetaspaceSize`, `-XX:MaxMetaspaceSize`.

### ⏳ 96. Что такое Memory Tuning?
- Выбор GC (`-XX:+UseG1GC`, `-XX:+UseZGC`)
- Размеры Heap: `-Xms`, `-Xmx`
- TLAB, String Deduplication, `-XX:+UseCompressedOops`

### 🔄 98. Что такое Compressed Oops?
- Оптимизация указателей в 64-битных JVM.
- Сохраняет память при Heap <32 ГБ.
- Включается флагом: `-XX:+UseCompressedOops`

### 💡 114. Как работает Metaspace?
**Ответ:** С Java 8: замена PermGen. Использует native memory, динамически растёт. Хранит информацию о классах, методах, константах.

### 💡 115. Как ограничить Metaspace?
**Ответ:** С помощью флагов:
```
-XX:MetaspaceSize=256m
-XX:MaxMetaspaceSize=512m
```
Полезно для контроля роста в системах с динамической загрузкой классов (например, OSGi, Groovy).

**Решение:** увеличить Heap, оптимизировать аллокации.

### 🧱 128. Как работает Remembered Set (RSet) в G1 и зачем он нужен?
**Ответ:**  
`RSet` — структура, которая содержит ссылки на объекты из других регионов в текущий.  
**Цель:**  
- Уменьшить необходимость полного прохода по Heap
- Повысить производительность GC

### 🧠 130. Как влияет размер кучи на частоту GC и производительность?
**Ответ:**
- Маленький Heap → Частые, но короткие GC
- Большой Heap → Редкие, но долгие GC
- Оптимум зависит от типа нагрузки:
  - Low-latency → ZGC, Shenandoah
  - High-throughput → G1, Parallel GC

### 🧩 135. Что такое Metaspace Fragmentation и как с этим бороться?
**Ответ:** Фрагментация Metaspace возникает, когда область метаданных заполняется "дырками" из-за загрузки/удаления классов.

- Симптом: `OutOfMemoryError: Metaspace`, даже при видимо свободной памяти.
- Решения:
  - Профилировать загрузку классов.
  - Использовать `-XX:MaxMetaspaceSize` и следить через jcmd.
  - Избегать динамической загрузки классов (например, через custom classloader).

- Повторяющиеся строки в Heap объединяются в одну.
- Работает в фоновом режиме при использовании G1.
- Полезно для приложений с большим количеством одинаковых строк (логирование, JSON, XML).

### 🧠 138. Что делает флаг `-XX:+AlwaysPreTouch`?
**Ответ:** Принудительно аллоцирует (протягивает) весь Heap при старте JVM.

- Ускоряет запуск приложений.
- Снижает потребление Metaspace.
- Включается флагами: `-Xshare:on`, `-XX:+UseAppCDS`

- Включается автоматически при Heap ≤ 32 ГБ.
- Экономит память в Metaspace.
- Управляется флагом: `-XX:+UseCompressedClassPointers`
### 🔁 141. Что такое Compaction в GC?

### 🔁 143. Как работает Heap Dump и как его проанализировать?

**Ответ:**  
Heap Dump — снимок памяти в момент времени.  
Создаётся с помощью:  
- `jmap -dump:live,format=b,file=heap.hprof <pid>`  
- `-XX:+HeapDumpOnOutOfMemoryError`

**Ответ:**  
CodeCache — область памяти вне Heap, куда JIT-компилятор помещает скомпилированный машинный код.  
Можно настроить через `-XX:ReservedCodeCacheSize`.

**Ответ:**  
Принудительно резервирует и заполняет память Heap при старте JVM. Уменьшает паузы во время работы, увеличивает время старта.

### 🧠 Вопрос 160: Что такое heap fragmentation и как её избежать?
**Ответ:**  
Фрагментация Heap — ситуация, когда свободная память есть, но она разбита на куски и не подходит для большого объекта.  
Решения:
- Компактирующие GC (Serial, G1)
- Тюнинг heap: увеличение -Xmx, настройка SurvivorRatio, использование G1/ZGC

### 🧠 163. Что такое PhantomReference?
**Ответ:**  
PhantomReference — это ссылка, при которой объект уже считается недостижимым, но ещё не удалён.  
Используется вместе с ReferenceQueue для контроля освобождения ресурсов вне Java Heap.

### 🧠 164. Какие типы OutOfMemoryError бывают?
**Ответ:**  
- Java heap space  
- GC overhead limit exceeded  
- Metaspace  
- Requested array size exceeds VM limit  
- Direct buffer memory  
- Unable to create new native thread

### 🧠 165. Что такое Unsafe.allocateMemory и чем он опасен?
**Ответ:**  
Метод `Unsafe.allocateMemory` выделяет память вне Heap, минуя все проверки JVM.  
Опасен утечками, требует ручного освобождения (`freeMemory`).  
Используется в high-performance системах (например, Netty, Cassandra).

### 🧠 166. Что такое Heap Fragmentation и как с ней борются?
**Ответ:**  
Фрагментация — ситуация, когда в Heap есть свободное место, но нет непрерывного блока нужного размера.  
Решения:
- Компактация (compaction)
- Алгоритмы GC с перемещением объектов (G1, ZGC)
- TLAB и region-based allocation

### 🧠 167. Что такое Compressed Class Pointers?
**Ответ:**  
Оптимизация JVM: уменьшает размер указателей на классы в Metaspace до 32 бит, если объём < 32ГБ.  
Активируется автоматически или через `-XX:+UseCompressedClassPointers`.

### 🧠 168. Как работает Stack Allocation через Escape Analysis?
**Ответ:**  
Если объект не покидает границ метода, JVM может разместить его в Stack, а не в Heap.  
Это уменьшает давление на GC.

### 🧠 169. Какие области памяти в JVM являются off-heap?
**Ответ:**  
- Direct ByteBuffers  
- MemoryMapped Files  
- Metaspace  
- TLAB (внутри Eden, но логически изолирован)
- Unsafe memory

### 🧠 170. Что такое -XX:+AlwaysPreTouch?
**Ответ:**  
Флаг JVM, при котором вся память Heap инициализируется сразу при запуске.  
Полезен в latency-sensitive системах, предотвращает задержки от page faults во время работы.

### ❓ 173. Чем отличается Metaspace от Code Cache?
**Ответ:**
- **Metaspace** — хранит метаданные классов (структуры классов, методы и т.п.).
- **Code Cache** — область памяти для JIT-скомпилированного нативного кода.

### ❓ 174. Как настроить максимальный размер Metaspace?
**Ответ:** Параметр `-XX:MaxMetaspaceSize=<size>` ограничивает размер Metaspace. Если не установлен — расширяется динамически.

### ❓ 177. Что такое Soft Max Heap Size и как он используется?
**Ответ:** JVM может использовать больше памяти, чем `-Xmx`, если доступно (в G1). Soft limit регулируется внутренне и может быть превышен в пиковые моменты.

### ❓ 178. Как отследить утечки памяти в Metaspace?
**Ответ:**
- Использовать `jcmd <pid> VM.native_memory summary`
- Анализ через VisualVM или Java Flight Recorder
- Проверить ClassLoader'ы, которые не выгружаются (memory leak)

### ❓ 179. Что такое Heap Dump и как его получить?
**Ответ:**
- `jmap -dump:live,format=b,file=heap.bin <pid>`
- `-XX:+HeapDumpOnOutOfMemoryError`
- Используется для анализа утечек и retained size

### 183. Что такое ObjectAlignment и как он влияет на Heap?

Heap fragmentation — это ситуация, при которой доступная память в heap фрагментирована и не может быть использована для крупных объектов.

### 194. Как работает `-XX:+PrintHeapAtGC`?

- Оптимизация JVM: сжатые указатели на метаинформацию классов.
- Включено по умолчанию при heap < 32 ГБ.
- Снижает потребление памяти Metaspace.

### 200. Как влияет Metaspace на OutOfMemoryError?

- Если исчерпано пространство под Metaspace → `OutOfMemoryError: Metaspace`.
- Причины: много классов, утечки ClassLoader’ов.
- Решение: увеличить `-XX:MaxMetaspaceSize`, исправить загрузку классов.

---
[Назад](README.md) 
